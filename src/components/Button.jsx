const Button = () => {
  return (
    <button className="bg-primary hover:bg-secondary text-white transition-all duration-1000 px-4 py-2 rounded-full uppercase cursor-pointer">
      Let’s Explore
    </button>
  );
};

export default Button;
